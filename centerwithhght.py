import netCDF4
import matplotlib.pyplot as plt
from glob import glob
import numpy as np
import ctables

#GOAL: Plot circulation center (if it actually exists) with height and time, using subplots to display the three classic cases versus the three HP cases

#general
#cases pre-2015
radar_elev = np.array([0.5, 0.9, 1.3, 1.8, 2.4, 3.1, 4.0, 5.1, 6.4, 8.0, 10.0, 12.5, 15.6, 19.5])

voltimes =

for voltime in voltimes:
    